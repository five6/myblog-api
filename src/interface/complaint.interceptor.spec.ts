import { ComplaintInterceptor } from './complaint.interceptor';

describe('ComplaintInterceptor', () => {
  it('should be defined', () => {
    expect(new ComplaintInterceptor()).toBeDefined();
  });
});
